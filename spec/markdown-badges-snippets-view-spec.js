'use babel';

import MarkdownBadgesSnippetsView from '../lib/markdown-badges-snippets-view';

describe('MarkdownBadgesSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
